let initialText = document.querySelector(".initialText")


window.addEventListener("keydown", (e) => {
    
    console.log(e.key)
    console.log(e.keyCode)
    console.log(e.code)
})

//TODO: Agregar el texto a cada div 